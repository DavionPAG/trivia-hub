let roundTimer = 15;
let interval;

const startTimer = () => {

interval = setInterval(() => {
   
    if (timer > 0) {
      timer --;
    } else if ($timer === 0) {
      clearInterval(interval);

    }
  
}, 1000);







}
